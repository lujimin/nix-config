# macOS output examples

The files in this directory preserve the upstream `fern` and `frieren` configurations as
references. They are outside `src/`, so Haumea does not include them in this flake's active
`darwinConfigurations`.

The active bootstrap configuration is `../src/MacBook-Pro-16.nix`.
